<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.fog.tj.mapper.MonthMapper">

    <select id="pageC" resultType="cn.fog.tj.entity.Month">
        select * from x_month
    </select>

    <select id="pageCC" resultType="cn.fog.tj.entity.Month">
        select * from x_month ${ew.customSqlSegment}
    </select>
</mapper>
